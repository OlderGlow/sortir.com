{{ form_start(form) }}

<div class="col-lg-12 col-md-6">
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
</div>

<div class="col-lg-12 col-md-6">
    <p class="form-submit">
        <button class="default-btn-one">Validé</button>
    </p>
</div>
{{ form_end(form) }}